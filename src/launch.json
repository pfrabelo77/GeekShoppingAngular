{
  "configurations": [
    {
      "type": "pwa-chrome",
      "request": "launch",
      "name": "Launch Angular App",
      "url": "http://localhost:4200",
      "webRoot": "${workspaceFolder}/src",
      "preLaunchTask": "npm: start"
    }
  ]
}